// Sirius Script samples

// 		How to create a default Object or Array?
// 		# = Push Command (Arrays, SiriusObject, functions arguments)
//						create a new object 
	#Object {
	}
//or
	#Object
//or
	#Object{}

// 		How to maniulate or create properties?
// string
mystringprop = value
// number
myintrop = 100
// boolean
mybooleanprop = true






// Behaviours
// Clone to id or select an behavior
~getbehaviour(id[,clonefromid])
current {
	propname = value
	someobject : Object {
		objectprop = value
	}
	somecustomtypedobject : MyObjectType {
		myobjectprop = value
		~callmyobjectmethod([param1,param2,...,paramN])
		~callmyobjectmethod2(
			#Object {
				someprop = x
			}
		)
	}
	~callbehaviourmethod()
}


// OBJECTS ==============================================================
// create or select an object by id
~createobject(id[,name])
//set grid map size
~setsize(1,2)
//set visual asset, and start frame
~setasset("test.swf"[,0])
//create a empty behavour list with an id or clone from existing one
~newbehaviourslist([id,cloneid])
//add one or more behaviours to specified type
~addbehaviours(type,behaviourA[,behaviourB,behavioursC,...,behaviourN])

